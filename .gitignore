# Terraform
**/.terraform/
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Sensitive variable files
terraform.tfvars
*.tfvars
*.tfvars.json

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI config (user-specific)
.terraformrc
terraform.rc

# Plan files
*.tfplan
*.tfplan.*
plan.out

# Provider plugins if accidentally downloaded into repo
terraform-provider-*

# Keep the dependency lock file tracked (do NOT ignore)
!*.terraform.lock.hcl

# Editors/OS noise (optional, safe defaults)
.vscode/
.idea/
.DS_Store
Thumbs.db
